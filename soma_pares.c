#include <stdio.h>
int main(){
int i = 0, cont = 0, v[10];

for (i = 0; i<10; i++)
    {
        printf("digite o valor para a posicao %d: ", i);
        scanf("%d", &v[i]);
    }

    if (i = 0, cont = 0; i<10; i++)
    {
        /* code */
    }
    
    return 0;
}